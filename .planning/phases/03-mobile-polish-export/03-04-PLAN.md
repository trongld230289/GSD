---
phase: 03-mobile-polish-export
plan: 04
type: execute
wave: 2
depends_on:
  - 03-02
  - 03-03
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - PWA-01

must_haves:
  truths:
    - "Content renders correctly on 320px–430px screen widths (no horizontal scroll, no clipped text)"
    - "App passes Lighthouse PWA audit with score >= 90"
    - "Bottom nav is visible above iPhone home indicator (safe-area fix confirmed working)"
    - "Export button triggers CSV download from a deployed app"
  artifacts: []
  key_links: []
---

<objective>
Verify that the code fixes from Plans 02 and 03 actually work in the real environment. This plan is a human-verification gate: responsive layout QA at four breakpoints and a Lighthouse PWA audit against the deployed GitHub Pages URL. Both require visual inspection or a live URL — they cannot be automated in-process.

Purpose: PWA-01 ("app fully usable on 320px–430px") and the Lighthouse score gate (>= 90) can only be confirmed against a real browser. Plans 02–03 make code changes; this plan confirms they produce the correct observable outcomes.
Output: Human confirms responsive layout passes and Lighthouse score meets the threshold. Phase 3 is complete when both checkpoints pass.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-mobile-polish-export/03-RESEARCH.md
@.planning/phases/03-mobile-polish-export/03-02-SUMMARY.md
@.planning/phases/03-mobile-polish-export/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and deploy — push changes to trigger GitHub Actions</name>
  <files></files>
  <action>
    Plans 02 and 03 made code changes. This task ensures the changes are committed, pushed to main, and deployed to GitHub Pages before the human verification steps run.

    Step 1 — Verify local build is clean:
    ```bash
    cd D:/Project/GSD/finance-tracker && npm run build
    ```
    Build must exit 0. If it fails, stop and diagnose before pushing.

    Step 2 — Confirm there are no uncommitted changes from Plans 02–03:
    ```bash
    git -C D:/Project/GSD status --short
    ```
    If Plans 02–03 were committed by their own executors (standard behavior), this should show a clean tree. If changes are unstaged, commit them:
    ```bash
    git -C D:/Project/GSD add finance-tracker/index.html finance-tracker/src/components/BottomNav.tsx finance-tracker/src/utils/exportCsv.ts finance-tracker/src/components/Header.tsx
    git -C D:/Project/GSD commit -m "feat(03): safe-area fix + CSV export"
    ```

    Step 3 — Push to main to trigger GitHub Actions deployment:
    ```bash
    git -C D:/Project/GSD push origin HEAD:main
    ```

    Step 4 — Wait for GitHub Actions to complete. Check status at:
    https://github.com/trongld230289/GSD/actions

    The deploy workflow (`deploy.yml`) triggers on push to main for changes under `finance-tracker/**`. Deployment takes approximately 2–3 minutes. Do NOT proceed to the verification checkpoint until the Actions job shows green.
  </action>
  <verify>
    <automated>git -C D:/Project/GSD log --oneline -3</automated>
  </verify>
  <done>Changes from Plans 02–03 are committed and pushed. GitHub Actions deployment workflow has completed successfully (green checkmark on https://github.com/trongld230289/GSD/actions).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Responsive QA pass at 320px–430px</name>
  <what-built>BottomNav safe-area fix (Plan 02) and all prior UI code. The fix ensures bottom nav is not obscured by the iPhone home indicator and removes the blocking zoom constraint from the viewport.</what-built>
  <how-to-verify>
    Open Chrome on desktop. Navigate to https://trongld230289.github.io/GSD/

    Open DevTools (F12) → click the device toolbar icon (Ctrl+Shift+M) → set to "Responsive" mode.

    Test at each of these four widths. At each width, navigate through Home, Budget, and Reports pages:

    **320px** (smallest Android / iPhone SE)
    - [ ] No horizontal scrollbar appears
    - [ ] Balance summary cards are readable (not clipped)
    - [ ] Transaction list rows do not overflow
    - [ ] Bottom nav tabs (Home, Budget, Reports) are all visible and tappable
    - [ ] FAB (+ button) is not obscured
    - [ ] Header shows Finance label and buttons without clipping

    **375px** (iPhone SE 2nd gen / iPhone 8)
    - [ ] Same checks as 320px

    **390px** (iPhone 14/15 standard)
    - [ ] Same checks as 320px

    **430px** (iPhone 14/15 Pro Max)
    - [ ] Same checks as 320px

    If any layout breaks are found, describe them (e.g., "at 320px the balance cards overflow horizontally"). Claude will create a follow-up fix before this checkpoint can be approved.
  </how-to-verify>
  <resume-signal>Type "responsive-ok" if all breakpoints pass, or describe any layout issues found at specific widths.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Lighthouse PWA audit — score >= 90</name>
  <what-built>PWA manifest, service worker (Plan 01), safe-area fixes (Plan 02). The Lighthouse audit verifies the full installability chain: manifest scope, icon sizes, service worker registration, and offline support.</what-built>
  <how-to-verify>
    **Option A — Chrome DevTools (easiest):**
    1. Open https://trongld230289.github.io/GSD/ in Chrome (not incognito)
    2. Open DevTools → Lighthouse tab
    3. Set: Categories = "Progressive Web App", Device = "Mobile"
    4. Click "Analyze page load"
    5. Wait for report (30–60 seconds)
    6. Note the PWA score and check the "Installable" section — all criteria should be green

    **Option B — Lighthouse CLI:**
    ```bash
    npx lighthouse https://trongld230289.github.io/GSD/ --only-categories=pwa --output=json --output-path=lighthouse-report.json
    ```
    Then check `lighthouse-report.json` for `"pwa"` category score (value between 0–1, multiply by 100).

    **Expected results:**
    - PWA score >= 90
    - "Installable" section: all green (no red/orange items)
    - "PWA Optimized" section: viewport meta passes (user-scalable=no was removed in Plan 02)

    **If score is below 90:** Note which specific criteria are failing (e.g., "Manifest does not have a maskable icon" or "Service worker does not handle fetch events"). Claude will address failing criteria in a follow-up fix.
  </how-to-verify>
  <resume-signal>Type "lighthouse-ok N" where N is the score (e.g., "lighthouse-ok 95"), or describe which Lighthouse criteria are failing.</resume-signal>
</task>

</tasks>

<verification>
Phase 3 gap closure is complete when:
1. Build exits 0 and changes are pushed to main
2. GitHub Actions deployment shows green
3. Responsive QA passes at all four breakpoints (320, 375, 390, 430px)
4. Lighthouse PWA score >= 90 with Installable section all green
5. CSV download works on the deployed URL (spot-check from Task 2 summary)
</verification>

<success_criteria>
- Deployed app at https://trongld230289.github.io/GSD/ passes responsive QA at 320px–430px
- Lighthouse PWA audit score >= 90
- All Phase 3 ROADMAP "Done when" items are now verifiable as complete:
  - App installable on Android (PWA-02 — from Plan 01)
  - App installable on iOS (PWA-02 — from Plan 01)
  - Lighthouse score >= 90 (PWA-01 — confirmed in this plan)
  - Content renders 320–430px (PWA-01 — confirmed in this plan)
  - iPhone safe area handled (PWA-01 — fixed in Plan 02)
  - Export to CSV works (EXPORT-01, EXPORT-02 — implemented in Plan 03)
  - GitHub Actions auto-deploys (already verified in original VERIFICATION.md)
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-polish-export/03-04-SUMMARY.md` with:
- Responsive QA results at each breakpoint (pass/any issues found)
- Lighthouse score achieved
- Any follow-up fixes applied during this plan
- Confirmation that Phase 3 is complete
</output>
